# Kelp One-Pager Agent — DEPLOYED Streamlit app + sector-only generator.
# Runtime deps only, kept minimal so the Streamlit Cloud build is fast.
#
# Intentionally omitted (not needed by the deployed app):
#   anthropic / google-genai  — imported lazily; only used if you route a skill
#                               off the LiteLLM gateway to those providers directly.
#   fastapi / uvicorn         — only for main.py (the separate FastAPI server).
#   typer / rich              — only for cli.py.
# Add any of these back if you deploy those entry points.

pydantic>=2.7
openai>=1.40
streamlit>=1.40
python-dotenv>=1.0
httpx>=0.27

# Local input-document parsing (input/ ground-truth files)
pdfplumber>=0.11
python-docx>=1.1
